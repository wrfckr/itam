<div class="w3-panel">
	<form action="/asset/add" method="get">
		<button class="w3-button w3-round-large w3-teal" type="submit">Добавить ассет</button>
	</form>
</div>
<table id="example" class="w3-table-all w3-card-4">
	<thead>
		<tr>
			<th>Hostname</th>
			<th>IP-адрес</th>
			<th>Пользователь</th>
			<th>Действия</th>
		</tr>
	</thead>
	<tbody>
		<% tableData.forEach(item => { %>
		<tr>
			<td><%= item.hostname %></td>
			<td><%= item.ip %></td>
			<td><%= item.name %></td>
			<td>
				<div class="w3-bar">
					<form action="/asset/edit/<%= item.id %> " method="get">
						<button
							id="dtnEdit"
							class="w3-button w3-orange"
							style="width: 100px"
							type="submit"
						>
							Изменить
						</button>
					</form>

					<form action="/asset/delete/<%= item.id %> " method="get">
						<button class="w3-button w3-red" style="width: 100px" type="submit">
							Удалить
						</button>
					</form>
				</div>
			</td>
		</tr>
		<% }); %>
	</tbody>

	<br />
</table>
